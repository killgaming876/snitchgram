"use client";
import Link from "next/link"; import { useState } from "react"; import { createClient } from "@/lib/supabase/client";
export default function RegisterPage(){const [username,setUsername]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const supabase=createClient();if(!supabase){window.location.href='/onboarding';return}const {error}=await supabase.auth.signUp({email,password,options:{data:{username}}});if(error)setError(error.message);else window.location.href='/onboarding';setBusy(false)}return <div className="product-page"><div className="page-inner" style={{maxWidth:560}}><div className="page-heading"><div><div className="eyebrow"><span className="dot"/> create your node</div><h2>Enter the world.</h2></div><p>Your first profile becomes the center of your social graph.</p></div><form className="side-panel" onSubmit={submit}><label>Username<input required value={username} onChange={e=>setUsername(e.target.value)} placeholder="yourname"/></label><label style={{display:'block',marginTop:14}}>Email<input required value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com"/></label><label style={{display:'block',marginTop:14}}>Password<input required value={password} onChange={e=>setPassword(e.target.value)} minLength={8} type="password" placeholder="8+ characters"/></label>{error&&<p style={{color:'#ff8e6e',fontSize:11}}>{error}</p>}<button disabled={busy} className="btn btn-primary" style={{width:'100%',marginTop:18}}>{busy?'Creating…':'Create account ↗'}</button><p style={{textAlign:'center',color:'#777',fontSize:11}}>Already connected? <Link href="/login" style={{color:'#ffd43b'}}>Sign in</Link></p></form></div></div>}
