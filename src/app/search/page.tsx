"use client";
import { useState } from "react";
export default function SearchPage(){const [q,setQ]=useState('');const names=['maya','zane','ria','noah','kira'];const results=names.filter(n=>n.includes(q.toLowerCase()));return <div className="product-page"><div className="page-inner" style={{maxWidth:800}}><div className="page-heading"><div><div className="eyebrow"><span className="dot"/> search the graph</div><h2>Find a signal.</h2></div></div><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search people, posts, interests…"/><div className="side-panel" style={{marginTop:14}}>{(q?results:names).map(n=><div className="suggest" key={n}><div className="avatar">{n[0].toUpperCase()}</div><div className="suggest-info"><b>{n}</b><span>creator · active in your graph</span></div></div>)}{q&&!results.length&&<p style={{color:'#777'}}>No results. Try another signal.</p>}</div></div></div>}
