"use client";
import Link from "next/link"; import { useState } from "react"; import { createClient } from "@/lib/supabase/client";
export default function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const supabase=createClient();if(!supabase){window.location.href='/home';return}const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);else window.location.href='/home';setBusy(false)}return <div className="product-page"><div className="page-inner" style={{maxWidth:520}}><div className="page-heading"><div><div className="eyebrow"><span className="dot"/> return to the world</div><h2>Welcome back.</h2></div></div><form className="side-panel" onSubmit={submit}><label>Email<input required value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com"/></label><label style={{display:'block',marginTop:16}}>Password<input required value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="••••••••"/></label>{error&&<p style={{color:'#ff8e6e',fontSize:11}}>{error}</p>}<button disabled={busy} className="btn btn-primary" style={{width:'100%',marginTop:18}}>{busy?'Connecting…':'Enter SnitchGram ↗'}</button><p style={{textAlign:'center',color:'#777',fontSize:11}}>New here? <Link href="/register" style={{color:'#ffd43b'}}>Create an account</Link></p></form></div></div>}
