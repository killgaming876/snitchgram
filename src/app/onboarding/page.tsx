"use client";
import { useState } from "react"; import Link from "next/link";
const steps=["Username","Display name","Profile signal","Interests","Suggested people"];
export default function Onboarding(){const [step,setStep]=useState(0); return <div className="product-page"><div className="page-inner" style={{maxWidth:700}}><div className="eyebrow"><span className="dot"/> onboarding / {step+1} of {steps.length}</div><h2 style={{fontSize:'clamp(42px,7vw,78px)',letterSpacing:'-.06em'}}>{steps[step]}</h2><div className="side-panel" style={{marginTop:24}}><div style={{height:4,background:'#24241f',borderRadius:10,overflow:'hidden'}}><div style={{width:`${((step+1)/steps.length)*100}%`,height:'100%',background:'#ffd43b',transition:'.4s'}}/></div><p style={{color:'#8d8c84',fontSize:13,lineHeight:1.7,marginTop:24}}>Shape your presence one signal at a time. The visual layer evolves as you move through the setup.</p><input placeholder={step===0?'choose a username':step===1?'how should people call you?':'type or choose'} autoFocus/><button className="btn btn-primary" style={{width:'100%',marginTop:16}} onClick={()=>step<steps.length-1?setStep(step+1):null}>{step<steps.length-1?'Continue ↗':'Finish →'}</button>{step===steps.length-1&&<Link href="/home" className="btn btn-ghost" style={{display:'block',textAlign:'center',marginTop:10}}>Enter Home</Link>}</div></div></div>}
