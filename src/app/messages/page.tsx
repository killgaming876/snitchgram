import Link from "next/link";
const chats=['Maya Chen','Zane Cole','Ria Kapoor','Noah Williams'];
export default function Messages(){return <div className="product-page"><div className="page-inner" style={{maxWidth:900}}><div className="page-heading"><div><div className="eyebrow"><span className="dot"/> realtime</div><h2>Messages.</h2></div><p>Conversations are intentionally calm. Presence, typing and delivery can sit inside the product without taking over the screen.</p></div><div className="side-panel">{chats.map((c,i)=><Link href={`/messages/${i+1}`} className="suggest" key={c}><div className="avatar">{c[0]}</div><div className="suggest-info"><b>{c}</b><span>{i?'Last message 18m ago':'Active now · typing…'}</span></div><span style={{color:'#ffd43b',fontSize:10}}>{i?'':'●'}</span></Link>)}</div></div></div>}
