"use client"; import {useState} from 'react';
export default function NotificationSettings(){const [enabled,setEnabled]=useState(true);return <div className="product-page"><div className="page-inner" style={{maxWidth:760}}><div className="page-heading"><div><div className="eyebrow"><span className="dot"/> settings / notifications</div><h2>Notifications.</h2></div></div><div className="side-panel"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><span style={{fontSize:12}}>Social activity notifications</span><button className="btn btn-ghost" onClick={()=>setEnabled(!enabled)}>{enabled?'Enabled':'Disabled'}</button></div></div></div></div>}
