"use client";
import { useState } from "react";
export default function CreatePage(){const [published,setPublished]=useState(false);return <div className="product-page"><div className="page-inner" style={{maxWidth:900}}><div className="page-heading"><div><div className="eyebrow"><span className="dot"/> creation stage</div><h2>Make something.</h2></div><p>Upload, preview and publish. The visual stage is ready for a real Supabase Storage pipeline.</p></div><div className="side-panel"><div className="media" style={{borderRadius:18}}/><input type="file" accept="image/*,video/*" style={{marginTop:16}}/><textarea placeholder="Write a caption…" style={{marginTop:12,minHeight:100}}/><div style={{display:'flex',justifyContent:'flex-end',marginTop:14}}><button className="btn btn-primary" onClick={()=>setPublished(true)}>{published?'Published ✓':'Publish ↗'}</button></div>{published&&<p style={{color:'#ffd43b',fontSize:11}}>Your post entered the feed.</p>}</div></div></div>}
